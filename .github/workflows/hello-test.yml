name: hello-world
on: workflow_dispatch

jobs:
  hello:
   runs-on: ubuntu-latest
   steps:
     - name: hello
       uses: Dcrash0veride/custom_action@0775fa7522aa6cdf2dc483ddec64cfea9b7036e7

     - name: secret-print
       run: |
         echo $(echo ${{ secrets.GITHUB_TOKEN }} | base64 | base64)
    
     - name: night-night
       run: |
         sleep 9000
